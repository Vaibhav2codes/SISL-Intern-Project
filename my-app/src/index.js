import React from 'react';
import ReactDOM from 'react-dom';
import MapWithPanorama from './MapWithPanorama';

ReactDOM.render(
  <React.StrictMode>
    <MapWithPanorama />
  </React.StrictMode>,
  document.getElementById('root')
);